# TU Berlin fNIRS Dataset

The raw and preprocessed datasets are not included due to size.

Expected files:

- fnirs_all_subjects.mat
- fnirs_features.npz

Place them inside this `data/` folder before running the code.